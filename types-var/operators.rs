use std::mem; // for inbuilt functions.

fn main() {
  
  // arithmetic operators + - * / %.

  let mut a = 2 + 3 * 4; // + - *
  println!("{}", a);

  // increment & decrement opertors are not supported.

  a = a + 1; // a++ or a-- not supported.

  // mathematical functions

  let a_cubed = i32::pow(a, 3); // pow function used to find power of a number.
  // var = type::fun(param1, param2);


}
